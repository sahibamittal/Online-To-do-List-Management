<%= form_with(model: shared_task, local: true) do |form| %>
  <% if shared_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shared_task.errors.count, "error") %> prohibited this shared_task from being saved:</h2>

      <ul>
      <% shared_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.number_field :priority %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
